This firmware is based off of qmk, and uses a custom python script to convert an mp4 file into a bin file.  
Use the python script to convert the mp4 file, and upload it to the qmk folder when compiling to use the file.
Both the bin file from the python code and qmk is required to run this code.